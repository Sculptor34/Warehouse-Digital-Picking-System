# SQL Server Management Studio

# Overview
This project demonstrates the basic SQL commands used in daily operations.

# Daily command
1. select * from t_pick_adhoc_detail
   where order_number =''
   and item_number =''

2. update t_pick_adhoc_detail set planned_quantity = 
   where order_number =''
   and item_number =''
   and pick_id =''

3. select len ('')
   
4. delete t_pick_adhoc_detail
   where order_number = ''
   and pick_id = ''

5. select * from t_pick_detail_info 
   where full_barcode like '%2665%'

6. select * from t_pick_detail_info
   where item_number = ''
   and cast(picked_date as date) = cast('' as date)



